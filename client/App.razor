@page "/"
@using System.Threading.Tasks

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        new LegendOfWorlds.Engine.World(JSRuntime);
        LegendOfWorlds.Engine.World.Init();
    }
}
